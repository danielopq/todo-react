import './footer.css';

/**
 * Footer component for the website.
 * Displays a message indicating that the todo list items can be reordered using drag-and-drop.
 * 
 * @returns {JSX.Element} The Footer component.
 */
const Footer = () => {
    return (
        <footer>
            <p>Drag and drop to reorder list</p>
        </footer>
    );
};

export default Footer;
